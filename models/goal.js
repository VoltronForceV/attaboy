var config = require('../config');
/*var mysql = require('mysql');
var connection = mysql.createConnection({
    host     : 'localhost',
    user     : ,
    password : 'secret',
});

c

connection.connect();
*/

console.log(config);

var goal = (function()
{
    function add()
    {
        
    }

    function view()
    {

    }

    function list()
    {

    }

    return {
        add: add,
        view: view,
        list: list
    };
});
