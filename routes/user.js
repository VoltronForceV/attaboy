var user_model=require('../models/user');
var info = function(req,res){
   // req.session.user.user_id; //user id
},
    update = function(){
 
};

module.exports={
    info   : info,
    update : update
};
